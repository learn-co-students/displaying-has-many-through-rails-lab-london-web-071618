<h1> <%= @doctor.name %> </h1>
<h2> <%= @doctor.department %> </h2>

<ol>
 <% @appointments.each do |appt| %>
 <li><%= link_to appt.patient.name, patient_path(appt.patient) %>, <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </li>
   <% end %>
</ol>
