<h1><%=@doctor.name%><h1>

Appointments:
<ul><% @doctor.appointments.each do |appointment|%>
  <li> <%=appointment.nice_date%> | <%= link_to appointment.patient.name, patient_path(appointment.patient.id) %> </li>
  <%end%>
</ul>
