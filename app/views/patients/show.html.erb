<h1> <%= @patient.name %> </h1>

<ol>
 <% @appointments.each do |appt| %>
 <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %>, <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </li>
   <% end %>
</ol>
